import { accessProperty } from '../../../helpers/access-property';
import { module, test } from 'qunit';

module('Unit | Helper | access property');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = accessProperty(42);
  assert.ok(result);
});
